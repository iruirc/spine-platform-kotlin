# kotlin-setup — en

## error_not_kotlin_project
Not a Kotlin project (no `settings.gradle(.kts)`, `build.gradle(.kts)`, `pom.xml` or `module.yaml` found), so I left `## Stack` unset. To create a new project use `@spine-platform-kotlin:kotlin-init`.

## auq_axis_target_label
Target surface

## auq_axis_ui_label
UI toolkit

## auq_axis_async_label
Async approach

## auq_axis_di_label
Dependency Injection

## auq_axis_framework_label
Server or CLI framework

## auq_axis_architecture_label
Architecture

## auq_axis_build_label
Build tool

## auq_axis_baseline_label
Baseline

## auq_axis_tests_label
Test framework

## report_axis_unknown
Kept stack line `{old}` as it is — no axis by that name exists today. Nothing reads it; delete it, or rename it to a current axis.

## report_modules_written
Wrote {n} module line(s) into `## Modules` — those modules resolve to a different stack than the root.

## report_axis_renamed
Renamed stack line `{old}` to `{new}` — it carried a question's label instead of the axis's; the value is unchanged.
