# kotlin-setup — ru

## error_not_kotlin_project
Не Kotlin-проект (не найден `settings.gradle(.kts)`, `build.gradle(.kts)`, `pom.xml` или `module.yaml`), поэтому `## Stack` остался незаполненным. Для генерации нового используй `@spine-platform-kotlin:kotlin-init`.

## auq_axis_target_label
Целевая поверхность

## auq_axis_ui_label
UI-тулкит

## auq_axis_async_label
Async-подход

## auq_axis_di_label
Dependency Injection

## auq_axis_framework_label
Серверный или CLI-фреймворк

## auq_axis_architecture_label
Архитектура

## auq_axis_build_label
Система сборки

## auq_axis_baseline_label
Baseline

## auq_axis_tests_label
Тестовый фреймворк

## report_axis_unknown
Строка стека `{old}` оставлена как есть — оси с таким именем сегодня нет. Её никто не читает; удали её или переименуй в существующую ось.

## report_modules_written
В `## Modules` записано строк: {n} — эти модули резолвятся в стек, отличный от корневого.
